// Deepak Aggarwal, Coding Blocks
// deepak@codingblocks.com
#include <iostream>
using namespace std;
void foo(int arr[], int n);

int main() {
    int x = 10;
    int* ptr = NULL;

    cout << ptr << endl;

    // ptr = &x;

    // cout << "ptr "<< ptr << endl;
    // cout << "&x" << &x << endl;
    // cout << "x " << x << endl;
    // cout << "*ptr " << *ptr << endl;


    // int arr[10] = {};
    // int *p1 = &arr[0];
    // int *p2 = &arr[2];


    // cout << p1 << endl;
    // cout << p2 << endl;
    // cout << (double*)p2 - (double*)p1 << endl;

    // int arr[10] = {1 , 2, 3, 4, 5 , 6};
    // cout << arr << endl;
    // cout << &arr[0] << endl;

    // int* ptr = arr;

    // cout << *(ptr + 1) << endl;
    // cout << ptr[1] << endl;

    // ptr = ptr + 1;  //++ptr
    // arr = arr + 1;  //++arr
    // ++arr;

    // cout << sizeof(arr) << endl;
    // cout << sizeof(ptr) << endl;

    // foo(arr, n);
}

void foo(int x[100000000], int n){
    ;
}